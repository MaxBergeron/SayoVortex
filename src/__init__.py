"""SayoVortex src package initializer.

This file makes the `src` directory a Python package so absolute imports
like `from src.game_objects import HitObject` work when running the project
as a package (python -m src.main) or from the repository root.
"""
